.ui.grid
  .sixteen.wide.column
    %h1
      %i.circular.icon.globe
      My Workspaces
    .ui.stacked.segment
      %table.ui.table.segment
        %thead
          %tr
            %th
              Name
            %th
              Hourly rate
            %th
              Currency
            %th
              Role

        %tbody
          - @user_workspaces.each do |workspace|
            %tr
              %td
                = link_to external_toggl_workspace_path(workspace), target: '_blank' do
                  = workspace.name
              %td
                = workspace.default_hourly_rate
              %td
                = workspace.default_currency
              %td
                = workspace.admin ? "Manager" : "Worker"
